/**
 * Déclaration de types pour le correcteur d'urgence du mode sombre
 */

/**
 * Applique des correctifs d'urgence pour le mode sombre
 * Cette fonction cible spécifiquement les problèmes de rendu qui ne peuvent pas être
 * résolus avec CSS seul et applique des correctifs via JavaScript
 */
export function applyEmergencyDarkModeFixes(): void;

/**
 * Export par défaut de la fonction principale
 */
export default function applyEmergencyDarkModeFixes(): void;
